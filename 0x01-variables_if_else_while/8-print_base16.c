#include <stdio.h>

/**
 * main - this is where my actual code is
 * Return: 0
 */
int main(void)
{
	int j;

	char l;

	for (j = 0 ; j < 10; j++)
		putchar(j + '0');
	for (l = 'a' ; l <= 'f' ; l++)
		putchar(l);
	putchar('\n');
	return (0);



















}
