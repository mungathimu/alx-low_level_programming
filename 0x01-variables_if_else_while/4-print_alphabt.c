#include <stdio.h>

/**
 * main - this is the where my code is
 * Return: 0
 */
int main(void)
{
	char r;

	for (r = 'a' ; r <= 'z' ; r++)
		if (r != 'q' && r != 'e')
			putchar(r);
	putchar('\n');

	return (0);




















}
