#include "main.h"

/**
 * _islower - This is a function that prints lower case
 *
 * @c: This is the parameter that is to be printed
 * Return: 1 if it is a lower case and 0 if otherwise
 */


int _islower(int c)
{
	if (c > 96 && c < 123)
		return (1);
	else
		return (0);




}
